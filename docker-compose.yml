version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
    container_name: yellow-mart-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    env_file:
      - .env
    command: npm run dev -- --host
